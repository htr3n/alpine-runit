#!/bin/sh

PATH=/usr/local/bin:/usr/local/sbin:/bin:/sbin:/usr/bin:/usr/sbin

echo "2 :: This is the runsvdir task"

# use runsvdir to monitor the service directory
exec runsvdir -P /etc/service 'log: ............'
